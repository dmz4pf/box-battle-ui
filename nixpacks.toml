# Railway Nixpacks configuration
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --omit=dev || npm install --production"]

[phases.build]
cmds = []

[start]
cmd = "node server/websocket-server.js"
