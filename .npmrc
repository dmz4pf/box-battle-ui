# Ignore scripts that might fail during build
ignore-scripts=false

# Use shamefully-hoist to flatten dependencies
shamefully-hoist=true

# Prevent installing test-only packages
# node-linker=hoisted
